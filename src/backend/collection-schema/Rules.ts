export interface Rules {
    item_id: string;
    event_type_id: string;
    action_ids: string;
    priority: number;
    severity: number;
    id: string;
    timeframe: string;
    rule_type_id: string;
    conditions: string;
    label: string;
}
